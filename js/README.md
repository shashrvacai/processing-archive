# js

The browser half of the folder: p5.js and plain-canvas sketches, kept in the three buckets they sat in on disk. `01-JS` is the early p5 work with the exported gifs next to the code, `VisualStuff` is the numbered test series, `12-coding-challenge` is the Coding Train follow-alongs and where they wandered off. Vendored p5 builds are left in place so each folder opens as it was.

All code by Cai, apart from the library files and the challenges that started as someone else's idea.
